
import SwiftUI

struct Notes: View {
    
    var body: some View {
        HStack {
            Text("Notes").font(.largeTitle).bold()
        }
    }
    
}

struct Notes_Previews: PreviewProvider {
    static var previews: some View {
        Notes()
    }
}
